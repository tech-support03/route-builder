{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); if [ -n \"$f\" ]; then \"$HOME/.wakatime/wakatime-cli-linux-amd64\" --entity \"$f\" --project route-builder --plugin 'claude-code/2.0 claude-code-wakatime/1.0' --category 'ai coding' --write >/dev/null 2>&1; fi; exit 0"
          }
        ]
      }
    ]
  }
}
